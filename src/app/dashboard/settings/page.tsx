import { ClientSettingsPage } from "@/components/client-settings-page";

// Server component that renders a client component for settings functionality
export default function SettingsPage() {
  return <ClientSettingsPage />;
}
